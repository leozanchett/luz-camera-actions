name: Access Secret
on: [push]
jobs:
  access-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world action
        if: ${{ secrets.NOME != '' }}
        run : echo "Hello, world! ${{ secrets.NOME }}"
        